
export interface Account {
  email: string,
  jwt?: string
};

export const storeToken = (token: string) : void  => {
  return 
};

export const retrieveToken = () : string => {
  return "dummyValue";
};

